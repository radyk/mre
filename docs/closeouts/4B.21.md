# Session 4B.21 -- one board, contradictory answers

2026-07-30. Branch master. One defect class, taken from 4B.17's remaining five:
A5, and the fused count that produced it.

Pinned world: rolling-c362baa4-1b0 (run c362baa4-1b03-4f6c-b3a4-d092c341dbdf,
contract 1.11, snapshot snap-rolling). Every read is PERSISTED; PYTHONHASHSEED=0
throughout; NO SOLVE ANYWHERE except the guard's own module-scoped fixture,
which builds an 18-order rolling world in 0.8s under deterministic mode
(workers 1, seed 42).

GROUND TRUTH, established first and used by everything below:

    40  orders KNOWN to the plan          (demands in the snapshot)
    26  orders SCHEDULED in this window   (distinct work_orders on assignments)
    14  orders BEYOND THE HORIZON         (tray, minus anything placed)
    56  operations PLACED                 (of 88 DECLARED in the snapshot)
    45  committed operations + 11 active-window = the 56
     0  excluded by the gate

    26 + 14 + 0 == 40.  The partition holds.

======================================================================
ITEM 1 -- THE CENSUS
======================================================================

----------------------------------------------------------------------
1(a). METHOD, AND WHY IT IS NOT A GREP
----------------------------------------------------------------------

The brief warned that three consecutive sessions had been mis-briefed by fusing
an incidental property of a known specimen into the class definition. So the
population came from the PROPERTY -- a surface reports a count of orders or
operations, or a disposition of them -- and was enumerated by MECHANISM.

STAGE A. An AST walk over every .py file in src/ (93 files), six mechanisms:

    C1  len(...) over something entity-ish            119
    C2  sum(...) over something entity-ish             25
    C3  Counter / max / min / .count over the same      7
    C4  an f-string carrying an entity noun AND a slot 286
    C5  a ratio with an entity-ish operand              19
    C6  a template constant carrying an entity noun     86
                                                      ----
        raw sites (deduped by file+line+mechanism)     542

STAGE B, THE BINDING PASS. A raw site is not yet a reported count. Two
questions decide it -- WHAT is aggregated, and WHERE does it land:

    542 raw sites
      224  aggregate ORDERS or OPERATIONS
      132  MIXED (an entity noun beside a non-entity one)
      186  something else (machines, records, findings, windows, findings)

      393  land in a SURFACE-PRODUCING file (24 files: the renderers, the
           opener, the rolling copy, the toolbox, the assembler, the API)
      149  land in solver / adapter / extractor internals

    CANDIDATE POPULATION = (orders|operations) x (surface)        253

STAGE C, THE PROSE PASS. Of the 253, which are sentences a planner reads?

    COUNT_IN_PROSE     47   a numeric slot governing an entity noun
    UNIVERSAL          11   a quantifier over an entity noun
    DISPOSITION_ONLY   17   names a disposition, states no count
    NOT PROSE         178   a len() in a loop bound, a dict key, a guard
                      ----
                      253

STAGE D, THE UNIVERSAL SWEEP -- SEPARATELY, AND THIS IS THE ONE THAT MATTERED.
Stage A's mechanisms only see a TEMPLATED string (C6 requires a "{"), and the
sharpest specimen in the whole census is

    "Every order finishes on time."

which has no slot, no number, and nothing on its surface saying what set it
ranges over. THAT IS PRECISELY WHY IT SURVIVED FOUR SESSIONS. So a second pass
read EVERY string constant in the 24 surface files (docstrings excluded --
they are not a planner surface) for a universal quantifier governing an entity
noun:

    universal strings, planner-facing                   38
    ...carrying a PLACEMENT-PRESUPPOSING predicate      15
    ...of which DEFECTS                                  3

----------------------------------------------------------------------
1(b). THE TABLE. EVERY SURFACE, RUN AGAINST THE PINNED BOARD.
----------------------------------------------------------------------

Each row was CALLED, not read. Value is what it reported on this board.

  SURFACE                                     COUNTS              VALUE  CALLS IT
  ------------------------------------------- ------------------- ----- ----------------------------------------
  inventory.order_count                       known demands          40  "N order(s) are in the plan, scheduled   [D1]
                                                                         across M operation(s)"
  inventory.operation_count                   placed rows            56  "...scheduled across M operation(s)"     [D1]
  inventory.splittable_op_count               splittable of 88        3  "N operation(s) can split across a       [D2]
                                                                         pause"
  inventory.late_count -> else-branch         lateness metrics        0  "Every order finishes on time."          [D3]
  lateness-cause.total_orders                 known demands          40  "{late} of {total} orders are late"      [D4]
  lateness-cause.on_time_count                total - late           40  "The other {n} finish on time or early"  [D4]
  lateness-cause, late==0                     --                     --  "Nothing is late in this plan -- every   [D5]
                                                                         order finishes on or before its due date"
  briefing.opener_scope.orders                placed work_orders     26  "N orders on M machines over <window>"   [D6]
  briefing fallback (pre-opener)              --                     --  "Nothing is late today -- every order    [D7]
                                                                         is on track."      (unreachable today)
  unknown-entity.order_total                  all snapshot orders    40  "There are N orders in this plan"        [D8]
  late-orders scope clause (total)            sched + tray           40  "Of the N orders on this plan"           [D9]
  ------------------------------------------- ------------------- ----- ----------------------------------------
  late-orders.scheduled_order_count           placed work_orders     26  "the N scheduled in this window"         OK
  late-orders.not_scheduled_order_count       tray minus placed      14  "the other N sit beyond the horizon      OK
                                                                         with no placement yet"
  late-orders.late_count                      lateness metrics        0  "No late orders found in this schedule." OK
  board_opener band `unplaced`                tray                   14  "N known order(s) sit beyond the         OK
                                                                         planning horizon with no placement yet"
  board_opener band `late` (clean)            --                     --  "Nothing in this window is late."        OK
  board_opener band `at_risk`                 at-risk orders          2  "N order(s) finish on time with less     OK
                                                                         slack than one of their own operations"
  rolling_questions.answer_beyond_horizon     len(beyond_horizon)    14  "N orders are known but not yet          OK
                                                                         scheduled -- they sit beyond the
                                                                         current window"
  rolling_questions.answer_frozen (committed) rolling.committed      45  "N operation(s) are frozen and           OK
                                                                         committed"
  rolling_questions.answer_frozen (active)    rolling.active         11  "Another N operation(s) are active in    OK
                                                                         the current window"
  rolling_questions.answer_coarse_fit         tray                   14  "all N orders beyond the horizon"        OK
  schedule (full listing)                     rows                   56  "The full schedule -- N operation(s)     OK
                                                                         across M machine(s)"
  machine-schedule CUT-01                     rows on one machine    18  "CUT-01: N operation(s)"                 OK
  machine-count                               resources              15  "15 machine(s) in this plant; 10 of      OK
                                                                         them carry work in this plan"
  integrity-check.op_count                    rows checked           56  "(N operation(s) checked)"               OK
  excluded_summary (renderers)                --                   None  (nothing excluded -- the note is         OK
                                                                         suppressed entirely)
  evidence_tools.lateness_set note            --                     --  "14 of 40 order(s) have NO placement in  OK
                                                                         this schedule and therefore no service
                                                                         outcome -- they are neither late nor on
                                                                         time. Lateness is stated for the 26
                                                                         scheduled order(s) only."
  evidence_tools.lateness_set.summary         all five figures    40/26/  {known/scheduled/late/on_time_or_early/  OK*
                                                                 0/26/14   not_scheduled}
  evidence_tools.entity_vocabulary.summary    order NAMES            40  {"orders": 40}                           OK*
  evidence_tools.cost_ledger.summary          assignments/outcomes 56/26  {"assignments", "service_outcomes"}      OK
  evidence_tools.machine_occupancy CUT-01     spans                  18  {"spans": 18}                            OK
  cockpit tray.js header count                beyond_horizon.length  14  "Beyond the horizon"  (title: "known     OK
                                                                         work not yet in a window")
  cockpit top strip                           --                     --  states NO order or operation count       OK
  conformance rates (order->product etc.)     SUBMISSION rows        40  "order->product resolution rate N%"      OUT
  ------------------------------------------- ------------------- ----- ----------------------------------------

  OK*  = correct, and named in the tool's own note or meaning, but the SUMMARY
         KEY was the bare word "orders". Fixed anyway (-> `known_orders`),
         because a reasoner quoting a key is not reading the note.
  OUT  = out of the class. These count SUBMISSION ROWS, not plan dispositions --
         a different set with a different authority. Named rather than silently
         passed.

----------------------------------------------------------------------
1(c). THE DEFECT COUNT: EIGHT FROM THE CENSUS, A NINTH FROM THE GUARD
----------------------------------------------------------------------

THE TEST APPLIED: does the sentence a planner reads make a claim that a
DIFFERENT surface on the SAME BOARD contradicts?

  D1  inventory lead. 40 is the KNOWN set; "scheduled" is false of 14 of them.
      56 is the PLACED set, belonging to the other 26. The word "scheduled"
      joins two sets that no code relates. CONTRADICTED BY: the opener (26),
      the tray (14), lateness_set's note (verbatim).

  D2  inventory splittable line. 3 of 88 DECLARED operations, spoken one line
      under a sentence that just said 56. Two denominators, adjacent, neither
      named -- and one of the three splittable operations has no placement at
      all, so the reader who takes "3 of the 56" is wrong about a real number.

  D3  inventory "Every order finishes on time." A universal over 40 where 26
      have a projected completion. CONTRADICTED BY: lateness_set, which says
      the other 14 "are neither late nor on time".

  D4  lateness-cause denominators. total_orders = 40; on_time_count = 40. The
      copy reads "{late} of {total} orders are late" and "The other {on_time}
      finish on time or early". LATENT on this board (late == 0 short-circuits
      to D5), LIVE on any board with late work.

  D5  lateness-cause, nothing late: "Nothing is late in this plan -- every
      order finishes on or before its due date." Same universal as D3, a
      DIFFERENT copy string in a DIFFERENT module. THIS IS THE MEMBER A BRIEF
      SAYING "FIX INVENTORY" WOULD HAVE MISSED.

  D6  the opener's scope line: "26 orders on 15 machines". Correct figure, bare
      noun. It is the OTHER half of D1's contradiction -- one of the two must
      name its set, and neither did.

  D7  the pre-opener briefing fallback: "Nothing is late today -- every order
      is on track." UNREACHABLE today (`_build_opener` always returns at least
      the two band-4 items, so the fallback branch is dead), and fixed anyway:
      a dead branch is a live branch after one refactor.

  D8  unknown-entity: "There are 40 orders in this plan". Bare noun, KNOWN set.

  AND ONE MORE, FOUND BY THE GUARD ON ITS FIRST RUN:

  D9  late-orders' own scope clause: "Of the 18 orders on this plan, that
      covers the 3 scheduled ... the other 15 sit beyond the horizon". 4B.13
      named the two SUBSETS and left their TOTAL bare -- the one figure in that
      sentence matching `inventory`'s headline was the one with no disposition
      on it. Nine, counting this.

----------------------------------------------------------------------
1(d). EVERY "EVERY / ALL / NONE OF THE N" SENTENCE, CHECKED
----------------------------------------------------------------------

The 15 planner-facing universals carrying a placement-presupposing predicate,
each with its verdict:

  ask_fallback_copy:65   "show every late order at a glance"        OK (offer label)
  ask_fallback_copy:518  "Nothing is late in this plan -- every ..."  DEFECT [D5]
  evidence_tools:342     "{order} has no scheduled operations ..."  OK (per order)
  explainer:48 / :1057   "are there any late orders"                OK (question text)
  explainer:4479         "I don't see any scheduled operations ..." OK
  renderers:805          "No late orders found in this schedule."   OK -- the 4B.13
                                                                    scope clause is
                                                                    appended on a
                                                                    rolling board
  renderers:1007         "I don't see any scheduled operations ..." OK
  renderers:1124         "Every order finishes on time."            DEFECT [D3]
  renderers:1134         "no two operations ... (N checked)"        OK -- names its
                                                                    own denominator
  renderers:1233         "explain why any order is late"            OK (offer label)
  renderers:1335         "It doesn't mean every order is on time"   OK (a disclaimer
                                                                    ABOUT the limit)
  renderers:2397         "Nothing is late today -- every order ..." DEFECT [D7]
  rolling_questions:518  "no operation has a start time yet"        OK (scoped to
                                                                    the coarse zone)
  rolling_questions:519  same sentence, second constant             OK

Three defects, twelve correct, and the twelve are correct for a reason that is
VISIBLE in each: they scope with "in this window", they name their denominator,
or they are labels rather than claims.

======================================================================
ITEM 2 -- THE RULING AND THE GUARD
======================================================================

Transcribed VERBATIM into docs/04 (2026-07-30), with the five-instance
observation the brief asked for. The census did not change the ruling's shape;
it ADDED two clauses, each with its reason:

  (3) TWO COUNTS SPOKEN IN ADJACENT SENTENCES SHARE A DENOMINATOR OR NAME THEIR
      OWN.  [from D2 -- adjacency asserts a relationship no code computed]
  (4) WHERE THE DISPOSITIONS DO NOT PARTITION THE KNOWN SET, THE SURFACE SAYS
      SO AND STATES NO TOTAL.  [4B.18's `CostProof.unreadable`, in a new place]

THE FIX IS ONE DEFINITION. `src/mre/modules/order_disposition.py`:
`census(explainer, document) -> OrderDisposition`, every field naming its set,
plus `partitions()` -- the 4B.3a completeness invariant restated where a
sentence is about to be built from it. Seven surfaces read it. No surface is
free to pick a denominator any more.

THE GUARD: `tests/test_cross_surface_counts.py`, 10 tests, TWO MECHANISMS.

  (1) CROSS-SURFACE AGREEMENT. 20 registered surfaces under 5 dispositions;
      surfaces sharing a disposition must agree, and must equal the census.
      A separate test asserts every disposition has at least TWO independent
      code paths -- a cross-surface test with one surface per disposition is a
      tautology.
  (2) THE PROSE TEST. A figure that is unambiguously one disposition must
      appear in a sentence carrying that disposition's word, and no universal
      over "orders" may stand unscoped.

ITS LIMIT, IN THE MODULE DOCSTRING: it watches routes through `Explainer.route`
and the toolbox summaries. NOT the cockpit's JavaScript (checked by hand,
correct), NOT the conformance gate's submission-row rates, and NOT a vague
sentence whose figure collides with no other disposition.

THE PREMISE TEST asserts known > scheduled > 0, a non-empty tray, and declared
operations > placed -- each separately, because a fixture can drift into
satisfying three of them. Without it every comparison is 40 == 40.

THE NEGATIVE CONTROLS, both run, both reverted:

  (a) the fused inventory sentence restored
        -> 3 failed (both prose tests + the named regression), 5 passed.
           THE AGREEMENT TEST STAYED GREEN -- every figure was still right.
  (b) the opener's scheduled count registered under KNOWN
        -> 1 failed (agreement), 7 passed.
           THE PROSE TESTS STAYED GREEN -- the words were still right.

Each half catches exactly what the other cannot. That is why there are two.

TWO THINGS THE GUARD CAUGHT ON ITS OWN FIRST RUNS, both kept:

  - D9 above, a real defect nobody had briefed.
  - ITS OWN FALSE POSITIVE. The fixture has 15 machines and 15 beyond-horizon
    orders, so "3 scheduled orders on 15 machines" was flagged as an unnamed
    BEYOND count. A bare value match cannot tell a machine count from an order
    count; the number must now GOVERN an entity noun. Recorded because a guard
    that cries wolf is one a future session learns to edit without reading.

======================================================================
ITEM 3 -- A5, CLOSED
======================================================================

THE DEFECT PROPERTY, confirmed: the chain and footer are assembled for the
ORDER; the lead is computed for the OPERATION. `_explain_why_on_machine` built
`ranked = on_machine + [everything else]` and passed the WHOLE list as
`ordered_records`. The lead read `ranked[0]`; the chain rendered all of it.

AND THE FOOTER CAME FROM THE SAME LIST. `api/app.py::_cited_refs_from_bundle`
walks `bundle.ordered_records` for the cockpit's lit bars, glowed lanes and
"alternatives weighed" line. So one list feeds four surfaces -- which is why
scoping it once fixes all four and they cannot drift apart again.

  (a) SCOPED. `why-on-machine` now takes an `op_seq`; the taxonomy's params
      were (order, machine), so a question naming "op20" had that word dropped
      at the dispatch and the route could not have scoped even if it wanted to.
      When the parse supplies no op_seq the route reads one off the asked
      question (`attribute_lookup.op_seq_in`, the existing reader). The order's
      OTHER steps leave the citation list and are NAMED in a sentence:
      narrowing silently is the same defect as widening silently.

      Measured: the chain went from 3 records to 1, and the footer's
      alternatives list from ["CUT-02", "CUT-03"] to empty. Lead, chain and
      footer now agree.

  (b) THE DRIVER PHRASE AS A WHOLE CAUSAL CLAUSE -- ITS THIRD SEAM, CENSUSED.
      Every site that renders a driver phrase as a standalone Why:

        renderers.py  ASSIGNMENT branch      "Why: {phrase}"      <- A5's
        renderers.py  catch-all branch       "Reason: {phrase}"
        explainer._explain_why_on_machine    the LEAD (fixed for CAPACITY_BLOCKED
                                             only, 4B.5 CU3; every other driver
                                             code still renders bare)
        explainer._record_gist               "the assignment decision -- {phrase}"
        explainer opener `common_cause`      "Common thread: {phrase}."
        explainer late-order `driver_phrase` a key fact, rendered by its route
        sandbox `dominant_driver.phrase`     the delta card

      THE TWO RENDERER SITES ARE THE CLASS, and both are fixed the same way:
      the label now claims only what is checkable. "Recorded driver: the
      machine was busy with other work" is TRUE -- the record's driver field
      says CAPACITY_BLOCKED. "Why: ..." claimed to be the reason for the
      placement, one line under a lead that said there was none. A label is a
      claim (4B.19's ruling), and this one was asserting a cause.

      Where the LEAD was computed from the solved occupancy, the chain now says
      so on the record it applies to, rather than standing as a rival
      explanation the reader has to adjudicate.

      The other five are named, not fixed: four are figures or short labels
      rather than whole causal clauses, and the lead's own partial fix
      (CAPACITY_BLOCKED only) is a wider question than this session's.

  (c) THE FOOTER CANNOT CONTRADICT THE LEAD, by construction rather than by
      check: `recorded_alternative_count` is computed from the SAME scoped
      list the footer is built from, and on the specimen it is 0, matching
      `only_option: True`.

======================================================================
ITEM 4 -- CONTRACT TERMS THAT ARE ALSO ORDINARY WORDS
======================================================================

  (a) THE VOCABULARY. The contract's disposition and window terms, with the
      everyday sense that misleads:

        horizon        THE CURRENT SCHEDULING WINDOW (7-14 days).
                       Everyday: how far ahead you can see -- i.e. the plan's
                       DATE EXTENT. THIS IS THE ONE THAT PRODUCED THE DEFECT.
        scheduled      has a PLACEMENT in this window.
                       Everyday: planned for, intended, on the books.
        committed      placed AND inside the frozen zone.
                       Everyday: promised to a customer. Nothing to do with it.
        frozen         our own solve state, not the floor's.
        active window  solved this window, not yet frozen.
                       Everyday: currently running on a machine.
        excluded       removed by the gate for a DATA DEFECT.
                       Everyday: left out, for any reason at all.
        late/on time   properties of a PLACEMENT. An unplaced order is NEITHER.
                       Everyday: an order with no plan is "going to be late".
        complete       an operation's work is finished. Distinct from the
                       COMPLETENESS INVARIANT, which is about document cover.
        window         the solve interval. Everyday: a period of opportunity.
        capacity       open calendar minutes, before any derate.
        duration       settled in 4B.20 (working time vs elapsed span).

  (b) REACHABILITY, MEASURED. `spec_lookup` serves the CURRENT tier only
      (docs/01, 05, 06). Before this session:

        "committed"                    ->  0 passages
        "beyond the horizon"           ->  cost_model.json and the rule registry
        "what does beyond-horizon mean"->  the same two, neither about it

      docs/01 said NOTHING about dispositions; docs/05 has zero occurrences;
      docs/06 mentions beyond-horizon only inside the `coarse_horizon`
      coefficient prose. docs/04 says it properly and is HISTORICAL tier,
      admitted for design-rationale and NEVER for a capability answer.

      SO: THE VOCABULARY THAT MOST NEEDED DISAMBIGUATING WAS DOCUMENTED ONLY IN
      A PYTHON DOCSTRING.

      ADDING IT WAS FREE. docs/01 is already in the CURRENT tier, so a new
      section plus `python tools/build_corpus_index.py` made it retrievable --
      no code, no tool, no tier change. docs/01 gains **§6.10 Demand
      disposition in a schedule**: the four states as a table, and explicitly
      the everyday senses that mislead. After: "committed" -> 1 passage, §6.10.

  (c) RE-ASKED. `synthesis_prompt.md` v4 -> v5, rule 12: reach for
      `spec_lookup` before reasoning about one of those words, and -- the
      standing rule -- WHEN A TOOL HANDS YOU A DISPOSITION WORD, THAT WORD IS
      THE ANSWER.

      "what's the biggest risk in this plan" now parses to the CONTRACTED
      opener on both live runs (intent=briefing, 0.92 and 0.95), which answers
      correctly and never reaches tier two -- so it does not measure rule 12.
      Forced to tier two, VERBATIM:

        "compare the risk of the unplaced work against the tight-slack orders
         and tell me which you would chase first"

        Of the 40 known orders, 26 have a placement in this schedule and 14
        have none -- ORD-000005, ... and ORD-000036 -- and those 14 are neither
        late nor on time, they simply have no service outcome yet.
        [synthesis -- read from: 5348876d, 12b5e06c, 01d65946]

      That is the exact inverse of the measured falsehood, from the same tier,
      on the same board. NOT PERFECT, and said plainly: the final claim adds
      "no path to on-time delivery within the next two to three weeks", which
      overstates -- beyond-horizon work enters a later window, and whether it
      is late is what the coarse look-ahead answers. It is labelled
      interpretive, which is the right register for it.

======================================================================
ITEM 5 -- THREE SMALL THINGS A STRANGER SEES
======================================================================

  (a) DIAGNOSTIC STRINGS, CENSUSED. Five could reach a planner:

        [LLM validation failed: <the check's own verdict>; fell back to template]
        [LLM validation failed: invented a value; fell back to authored text]
        [LLM error: <PythonExceptionClassName>; fell back to template]
        [LLM error: <PythonExceptionClassName>; fell back to authored text]
        [rendered by: template (LLM error) | register: ...]

      All five say the same thing to a planner -- the answer you are reading is
      the deterministic text, not the model's prose -- in vocabulary that
      belongs in a log. The rendered-by footer already states that fact, so the
      detail now goes to the renderer's `last_diagnostics` and the DEBUG log.
      ROUTED, NOT DELETED.

      AND THE TAG MEANT ITS OWN OPPOSITE. `template (LLM validated)` reads as
      "the LLM validated this answer"; it means the LLM's draft was rejected.
      It is now `template (model draft rejected)`. The exam sidecar keys its
      `validator` tripwire on that tag and was updated in the same commit,
      keeping the old forms so archived sweeps still parse.

      NOT a diagnostic and deliberately kept: `[rendered by: ... | register:
      ...]` itself. That is delivery provenance a planner is entitled to, and
      the cockpit surfaces the register as a chip.

  (b) RAW MINUTES, CENSUSED. Six planner-facing sites print a lateness in raw
      minutes:

        renderers schedule row      "-13817min early"     <- THE SPECIMEN
        renderers forced-alts       "{n} min late"                    FIXED
        renderers briefing fires    "{n} min late"                    FIXED
        renderers challenge route   "{n} minutes past its due date"   left
        explainer swap take         "{n} min late"                    left
        rolling_questions lateness  "{n} minutes past its due date"   left

      THE SPECIMEN IS THE SHARP ONE and the only one with a sign/word
      contradiction: a NEGATIVE number and the word "early", each encoding the
      same direction, at 1,440 units to the day. `board_opener._elapsed` moved
      to `planner_language.elapsed_minutes` -- ONE definition, imported by both
      -- and the opener's output is byte-unchanged. The three prose sites are
      LEFT, and for a reason: R-PD1 clause (4) states the tardiness floor and
      the controllable part in the SAME unit so they can be compared, and
      reformatting one of them breaks that.

      Every other surface was already correct: the opener (8h22m of slack), the
      job card (97h 1m), attribute lookup (`human_minutes`).

  (c) DRILL-DOWN ANAPHORA -- MECHANISM REPORTED, NOT FIXED (per the brief).
      "show me the evidence for that", after a contracted answer carrying a
      cited chain, returns "I don't have a claim of my own open to ground."

      THE MECHANISM: `interpreter.pick_claim` reads `answer.claims`, and the
      only thing session memory holds is `SynthesisMemory` -- "the last
      SYNTHESIS answer per session". A contracted route's `ordered_records`
      never enter it, so after a template answer there is literally nothing for
      "that" to resolve to. The copy is honest about it ("the records behind it
      are cited on it -- name the part you want walked").

      IT IS A CONTEXT-LADDER CHANGE: remembering contracted bundles per session
      touches the memory contract and the API ask path. Named and left.

======================================================================
ITEM 6 -- VERIFIED LIVE, VERBATIM
======================================================================

Pinned world, shipped defaults (parse claude-haiku-4-5, synthesis
claude-sonnet-5), one session, each question asked in order. Em-dashes
transliterated to "--"; nothing else changed, elided or reordered.

----------------------------------------------------------------------
1.  "how many orders are in this plan"        intent=inventory conf=0.95
----------------------------------------------------------------------
    40 order(s) are known to this plan. 26 of them are scheduled in this
    window, across 56 placed operation(s); the other 14 sit beyond the horizon
    with no placement yet.
    3 of the 88 declared operation(s) can split across a pause (e.g. an
    overnight closure); 2 of those are placed in this window.
    All 26 scheduled order(s) finish on time. The 14 beyond the horizon have no
    completion date yet, so they are neither late nor on time.
    [rendered by: template | register: testimony]

Four sets, four names, and the splittable line now carries its own denominator
AND says how many of the three are actually on this board.

----------------------------------------------------------------------
2.  "ok so no orders are partially placed, its all in or all out?"
                                              intent=inventory conf=0.92
----------------------------------------------------------------------
    I've now given you this same answer for two different questions, which
    probably means I'm not understanding what you're asking. Last time you
    asked: "how many orders are in this plan".

    40 order(s) are known to this plan. 26 of them are scheduled in this
    window, across 56 placed operation(s); the other 14 sit beyond the horizon
    with no placement yet.
    3 of the 88 declared operation(s) can split across a pause (e.g. an
    overnight closure); 2 of those are placed in this window.
    All 26 scheduled order(s) finish on time. The 14 beyond the horizon have no
    completion date yet, so they are neither late nor on time.

    If you're after a specific field on a specific job -- whether an operation
    is splittable, how long it takes, its due date, which machines can run it
    -- name the order and the field and I'll read it straight off the record.

    [rendered by: template | register: testimony]

THE ANSWER IS NOW RIGHT AND THE QUESTION IS STILL NOT ANSWERED. The planner
asked whether an order can be PARTIALLY placed. The route answers how many are
placed. The `deaf` rider fires correctly (same answer, different question) --
this is §5a.58's boundary doing its job, and the honest reading is that "is it
all-in or all-out per order" has no route. Reported, not fixed.

----------------------------------------------------------------------
3.  "why is ORD-000013 op20 on PAINT-01"      intent=why-on-machine conf=0.95
----------------------------------------------------------------------
    ORD-000013 is on PAINT-01 because that is the only machine qualified to run
    this step -- there was no alternative to weigh.
    This is about op20 on PAINT-01; the evidence below is that step's own
    assignment decision.
    ORD-000013 has 1 other step(s) in this window, each with its own machine
    and its own reason -- ask about one by naming it (e.g. "why is ORD-000013
    op<seq> on <machine>?").

    Evidence chain (1 record(s)):

    [1] DECISION  | ASSIGNMENT  (scheduling)
        Assigned to: PAINT-01
        Recorded driver: the machine was busy with other work
        (the sentence above this chain is computed from the solved occupancy
         and supersedes this code)
        Note: This is a reconstruction from the solved schedule.
        [record: 47f106af...]

    [rendered by: template | register: testimony]

A5 CLOSED. Compare 4B.17's measurement: three records, two of them about other
operations, two dollar figures belonging to a different machine, a footer
naming two alternatives, and the developer diagnostic printed underneath. The
cockpit footer for this answer is now "lit 1 bar(s) - on: PAINT-01" with no
alternatives line at all.

----------------------------------------------------------------------
4.  "what's the biggest risk in this plan"    intent=briefing conf=0.92
----------------------------------------------------------------------
    26 scheduled orders on 15 machines over 2026-01-05 to 2026-02-09. 4 things
    worth your attention, worst first:

    1. 2 order(s) finish on time with less slack than one of their own
       operations takes.
         ORD-000011 -- 8h22m of slack against a longest step of 25h01m of work
         ORD-000003 -- 14h04m of slack against a longest step of 20h01m of work
         -> Ask "when does <order> finish?" for the timeline, or "what would
            have to change for <order> to start earlier?".

    2. planned maintenance on Wednesday 2026-01-14 takes out ASM-01, CUT-01,
       FINISH-01, FINISH-02 and 5 more.
         -> Ask "is any maintenance scheduled?" for the calendar.

    3. 14 known order(s) sit beyond the planning horizon with no placement yet.
         The earliest of them is due 2026-01-20.
         -> Ask "what's beyond the horizon?" for the list, or "will it fit?"
            for the capacity read.

    4. No capacity margin is declared for this plant, so every capacity figure
       here assumes 100% of open time is usable.
       Declare refinements.coarse_horizon.capacity_derate in cost_model.json
       (docs/06 section 5.9) and the coarse figures derate with it.
         -> Ask "how do I declare a capacity margin?".

    And what is going right:
      - Nothing in this window is late.
      - The cost optimum is PROVED: no cheaper schedule exists for this window
        under the declared cost model.
    [rendered by: template | register: testimony]

IT DID NOT REACH SYNTHESIS. On both live runs this parsed to the contracted
opener, which ranks the 14 beyond-horizon orders THIRD and describes them
correctly. The measured drift was a tier-two answer; rule 12 is verified
separately in Item 4(c). The scope line now says "26 SCHEDULED orders".

----------------------------------------------------------------------
5.  "is CUT-01 overloaded"                    intent=machine-schedule conf=0.72
----------------------------------------------------------------------
    CUT-01: 18 operation(s):

      [CUT-01]
        seq= 10  2026-01-05 07:00 -> 2026-01-05 08:18  ORD-000016   9d 14h early
        ... (14 rows elided here ONLY, and only in this close-out) ...
        seq= 10  2026-01-08 14:36 -> 2026-01-12 15:37  ORD-000011   8h22m early
        seq= 10  2026-01-13 07:00 -> 2026-01-13 14:06  ORD-000013   9h48m early
        seq= 10  2026-01-15 07:00 -> 2026-01-15 13:54  ORD-000022   5d 10h early
        seq= 10  2026-01-15 13:54 -> 2026-01-19 09:55  ORD-000003  14h04m early
    [rendered by: template | register: testimony]

The formatting is fixed: "9d 14h early" where 4B.20 measured "-13817min early",
and "8h22m early" -- the same string the opener uses for the same order's slack,
which is what makes the two surfaces checkable against each other.

STILL TRUE AND STILL REPORTED: this route answers "is CUT-01 overloaded" with an
enumeration and no utilisation figure at all. 4B.20's section 5a.69, unchanged.

----------------------------------------------------------------------
6.  "what should I be worried about"          intent=briefing conf=0.95
----------------------------------------------------------------------
    [the deaf rider, then the SAME opener body as 4 above, then the
     attribute-lookup invitation]

REGRESSION: the opener body is IDENTICAL to 4B.20's measurement except for the
one word this session changed -- "26 scheduled orders" where it read "26
orders". Every figure, every rank, every pointer unchanged. That is what the
census predicted: the opener reads the placed set and always did; what it
lacked was the word.

The `deaf` rider fires because questions 4 and 6 legitimately share a route in
one session. Correct behaviour, an artifact of asking six questions in one
sitting.

======================================================================
ITEM 7 -- WHAT THE LIVE RUN CAUGHT THAT THE GUARD COULD NOT
======================================================================

THIS IS THE SESSION'S SECOND RESULT, and it is a lesson about guards.

The first live run of Item 6 question 1 returned:

    I can't give you a reliable total: the orders I can account for (0
    scheduled, 0 beyond the horizon, 0 excluded) do not add up to the 40 this
    plan knows about, so at least one order is unaccounted for. That is a
    defect in this schedule document, not a fact about the plant.

Every cross-surface test was GREEN at that moment. TWO DEFECTS, both mine, both
introduced this session:

  1. THE ASK PATH DOES NOT HAND THE DOCUMENT TO EVERY ROUTE. `interpreter`
     injects `params["document"]` from an ALLOW-LIST of intents, and
     `inventory` was not on it. Every test in the guard calls
     `Explainer.route(...)` with a document in hand, so the guard proved the
     ASSEMBLER and never the PATH.

  2. THE DOCUMENTLESS FALLBACK READ THE WRONG KEY. Enriched assignment rows
     carry `work_orders` (a LIST -- an operation can serve several demands
     through batching), not `order`. Reading `order` returned an empty set, so
     the census saw 0 scheduled against 40 known.

  3. AND `partitions()` TURNED THAT INTO A CLAIM. It returned a definite FALSE
     where the honest value is "I cannot tell" -- manufacturing an assertion
     about the schedule out of a missing argument. It is TRI-STATE now, with
     `document_read` distinguishing UNREADABLE from EMPTY, for exactly the
     reason `CostProof` has four states and not three (4B.18).

All three are fixed and all three are pinned, including an assertion over the
interpreter's allow-list itself, keyed on the PROPERTY (routes that report a
beyond-horizon figure) rather than on the four names.

A GUARD THAT SUPPLIES ITS OWN ARGUMENTS PROVES THE ASSEMBLER, NOT THE PATH.

======================================================================
WHAT WAS TEMPTING AND LEFT
======================================================================

  * DRILL-DOWN ANAPHORA (Item 5c). A context-ladder change, named per the
    brief. It is the most planner-visible thing left in this file.
  * THE OTHER FIVE DRIVER-PHRASE SITES (Item 3b). Four are figures or labels
    rather than whole causal clauses; the fifth is `why-on-machine`'s lead,
    which is enriched for CAPACITY_BLOCKED only -- every other driver code
    still renders as a bare phrase. That is a wider question than this session.
  * "IS IT ALL IN OR ALL OUT PER ORDER" (Item 6 q2) has no route. The answer
    given is true and is about something else.
  * TWO DISPOSITION QUESTIONS CLAIMED BY ROUTES THAT DO NOT ANSWER THEM
    (section 5a.75): `coarse-fit` and `coaching`. Vocabulary calls.
  * THREE PROSE SITES STILL PRINTING RAW MINUTES, for R-PD1 clause (4)
    comparability (Item 5b).
  * `machine-schedule`'s missing utilisation figure -- 4B.20's own carry, and
    Item 6 question 5 demonstrates it again unchanged.
  * The `repeat`/`deaf` string-identity boundary; the metric and
    input-manifest readers; the verifier's derived-claim limit; 4B.17's four
    drifts and ten conversational misses.
  * CLAUDE.md COMPRESSION. Now ~69k against a 40k phase-exit ceiling, and this
    session added to it. It is the largest single item owed at the next phase
    exit and has been named as such for six sessions.

======================================================================
ARTIFACTS
======================================================================

  src/mre/modules/order_disposition.py     NEW -- the one definition
  src/mre/modules/explainer.py             inventory, lateness-cause,
                                           why-on-machine scope, excluded-orders
  src/mre/modules/renderers.py             the inventory renderer, the scope
                                           line, the driver label, the
                                           diagnostics, the formatter
  src/mre/modules/ask_fallback_copy.py     authored copy (governed)
  src/mre/modules/interpreter.py           the document allow-list
  src/mre/modules/planner_language.py      elapsed_minutes (one definition)
  src/mre/modules/board_opener.py          imports it
  src/mre/modules/evidence_tools.py        known_orders in two summaries
  src/mre/contracts/synthesis.py           ENTITY_VOCABULARY meaning (governed)
  src/mre/modules/synthesis_prompt.md      v4 -> v5, rule 12 (governed)
  src/mre/ai_exam/sidecar.py               the validator tripwire's tag
  tests/test_cross_surface_counts.py       the class guard, 10 tests
  tests/test_truth_failures_4b13.py        the renamed summary key
  tests/test_board_opener.py               the scope line's new word
  docs/01-canonical-model-spec.md          section 6.10, the disposition vocabulary
  docs/04-design-history.md                the ruling, verbatim
  docs/07-roadmap.md                       v2.67, sections 5a.71-78
  src/mre/corpus_index.json                rebuilt after the docs edits

======================================================================
SUITE
======================================================================

Full non-slow suite, green against the final state. The wall cap forced the
batched form (files[i::4], -p no:randomly), run SEQUENTIALLY -- four concurrent
pytest processes produce contention flakes this repo already carries as standing
debt -- so the totals are stated and reconciled:

    batch 0   491 passed    62 skipped
    batch 1   520 passed    46 skipped
    batch 2   499 passed    23 skipped
    batch 3   591 passed   121 skipped
              ----------   -----------
              2101 passed  252 skipped   = 2353

    pytest tests/ -q --collect-only  ->  2353 tests collected.   MATCH.

An earlier batched run against an INTERMEDIATE state showed six failures, all of
them tests asserting the diagnostic strings and the count wording this session
deliberately changed (Item 5(a), and `unknown-entity`'s "40 known orders"). Each
was updated in the same commit as the change it guards -- the instrument follows
the vocabulary, not the reverse -- and each is annotated with the reason at the
assertion. The totals above are against the final state with those updates in.
