Q: what makes a job impossible to move at all
   route=synthesis register=synthesis intent=teaching conf=0.92
--------------------------------------------------------------------------
No contracted answer covers that one, so this is me reading the evidence directly — each line below says what backs it.

Nothing in this schedule is actually locked: the mechanism that makes a job immovable is a lock/frozen-assignment constraint (lock_type = frozen, pinned_resource or pinned_start), and no order here carries one — the schedule's one flagged issue is a data-quality warning about customer_weight defaults, not a lock.  [synthesis — read from: d42f78ad]
In this product, a job becomes immovable only through a frozen_assignment or pinned constraint declared in locks.csv (lock_type: frozen = immovable, pinned_resource, or pinned_start) — that is the in-core, proven mechanism for pinning; nothing else in the catalog removes an operation's mobility outright.  [general knowledge — how scheduling works in general, not a fact about this plan]
Short of an explicit lock, a job's placement can still be very constrained by other proven-in-core rules stacking together — finish-start precedence, release/due dates, disjunctive machine capacity, eligibility to alternate resources, and working calendars — but these narrow the feasible window rather than fixing it to one spot.  [general knowledge — how scheduling works in general, not a fact about this plan]
For example ORD-SPAN's operation on PACK-01 is split into 2 pieces, 901 minutes of work spread over a 1621-minute span with a 720-minute pause — that pause is calendar/capacity-driven, not a lock, and the job is still free to move if the planner acts on it; ORD-BOX likewise shows no lock, just two sequential operations on FEED-01 and BOX-01.  [synthesis — read from: ff50c0be, 37447146]

Where a line is marked general knowledge it draws on how scheduling and plants behave generally — not on this plan's records, so there is nothing here to check it against.

That is how it works in general — you know this plant and I don't, so if any of it doesn't match what you see here, say so and I'll look at what your board actually does.
[rendered by: synthesis (claude-sonnet-5) — 7 tool call(s) | register: synthesis]
