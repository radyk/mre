# Dev / test dependency lock (session 2.4 CU1).
#
# Installed ONLY in the Dockerfile `test` stage (FROM runtime) and by CI, never
# in the shipped runtime image. Pinned alongside requirements.lock so CI tests
# the exact layers the image ships plus a fixed test toolchain.

-r requirements.lock

# --- test runner ---
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
packaging==26.2

# --- API test client (httpx drives the FastAPI app) ---
httpx==0.28.1
httpcore==1.0.9
certifi==2026.6.17
