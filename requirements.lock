# Runtime dependency lock (docs/07 Phase 2, W4 / session 2.4 CU1).
#
# Pinned, provider-agnostic. Regenerate deliberately (never float): these are
# the exact versions the container image ships and CI tests against. The
# runtime image installs ONLY this file — dev/test tooling lives in
# requirements-dev.lock and never enters the shipped image.
#
# Grouped by the direct dependency that pulls each transitive in.

# --- API surface (FastAPI + ASGI server) ---
fastapi==0.139.0
starlette==1.3.1
uvicorn==0.51.0
python-multipart==0.0.32
click==8.4.2
h11==0.16.0
anyio==4.14.1
sniffio==1.3.1
idna==3.18

# --- contracts (Pydantic) ---
pydantic==2.13.4
pydantic-core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0

# --- remediation catalog (YAML loader for remediation-catalog-v1.yaml) ---
# Imported at module load by mre.catalog.models (shipped runtime code, reached
# by the certificate's remediation register). Was installed on the dev host but
# never locked — the shipped image could not import mre.modules.remediation
# until this was added (session 2.4b, first real container build).
pyyaml==6.0.3

# --- solver (OR-Tools CP-SAT) ---
ortools==9.15.6755
# numpy is a transitive OR-Tools dep. Pinned to the newest release installable
# on the SHIPPED interpreter (python:3.11-slim); 2.5.x requires Python >=3.12.
# Keep this consistent with the Dockerfile base when regenerating the lock on a
# newer dev host (session 2.4b: the lock had drifted to a 3.12+-only numpy).
numpy==2.4.6
protobuf==6.33.6
absl-py==2.5.0
immutabledict==4.3.1
