AI EXAM TRANSCRIPT
target      : rolling-c32a6140-b6b
snapshot    : snap-rolling
started     : 2026-08-05T18:34:24Z
llm mode    : live
questions   : 14
llm calls   : 27
door check  : live
parse       : parses=17 calls=17 retries=0 malformed=0 clarifies=1 unavailable=0 median=1545ms
synthesis   : answers=2 calls=10 tools=8 malformed=0 budget-out=0 unanswerable=0 median=26964ms p90=45019ms
claims      : 8 total  verified=0 interpretive=3 general-knowledge=1 failed-and-cut=4 deferred=0  ungrounded-load-bearing=4
latency     : route n=12 median=1689ms p90=2513ms | synthesis n=2 median=28881ms p90=47139ms
graded      : 14/14 expectations met
sidecar     : ungrounded-load-bearing=2
------------------------------------------------------------------------
# AI EXAM BANK — THE PRODUCT EXPLAINS ITS OWN WORDS (R-TE1)
# version  : v1
# generated: 2026-08-05  (Session 4A teaching-graft (d.3))
# world    : the DEMO BOARD `rolling-c32a6140-b6b`. What is graded is the
# SHAPE, not the ids.
# 
# ===========================================================================
# THE FOUNDING SPECIMEN IS BLOCK A, VERBATIM
# ===========================================================================
# 
# Founder freewheel round, 2026-08-04. The `solve-optimality` route — a
# CONTRACTED one — answered the cost-optimum question with the word "seed" nine
# times ("this board is the best of 3 seeded searches, the one at seed 44"), and
# the planner asked what it meant. Twice. Both refusals held a door handle that
# opened on the wrong room:
# 
# "what do you mean seed"          -> entity CLARIFY
# "...what does it mean to         -> capability coach, "I don't recognize
# change seeds"                      which capability you mean"
# 
# THE OPENER IS LOAD-BEARING AND MUST NOT BE DELETED. R-TE1's trigger contract
# is that we explain words WE SAID: without turn 1 emitting "seed", turns 2 and
# 3 are correctly NOT this intent, which block D grades on purpose. A bank that
# dropped the opener would grade the opposite of what it claims to.
# 
# WHAT IS GRADED HERE, and what is not. Routing and the relational forms
# (R-EX2). The BODY — that the definition is right, that it cites R-BK1, that
# the run figure is voiced — is `tests/test_glossary.py`, per the division:
# banks grade routes and relations, bodies belong to tests.
# ===========================================================================
# ===========================================================================
# A — THE FOUNDER PAIR (both refusals, both now the same route)
# 
# RECORDS=0 is the discriminator that matters. A glossary answer cites a RULING
# and a DOCUMENT, not this run's evidence records, so it opens nothing — and
# before R-EX2 "answered from the glossary" and "answered from the board" would
# have been the same EXPECT line.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=solve-optimality route=solve-optimality]
Q[43]: is this the cheapest possible plan
  route=solve-optimality  source=parse  conf=0.95  register=testimony  renderer=template — no evidence chain — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=solve-optimality  conf=0.95  6550ms
  expect: intent=solve-optimality route=solve-optimality  -> MET
  A:
    I can't prove it, and I'd rather say so than guess. The solver ran out of budget before it could close the bound, with a gap of 89.6% still open. The earliest-start tiebreak did not prove out, which does not affect the cost claim above.

    That gap is the limit of the PROOF, not a measure of how good the schedule is: it says a cheaper plan may exist and could be up to 89.6% cheaper. On large plants we have measured wide gaps over schedules whose actual cost barely moved.

    On "found": this board is the best of 3 seeded searches, the one at seed 44.
    What each reached: seed 42 $2,135,369.63, seed 43 $1,801,222.70, seed 44 $1,667,467.80.
    They land $467,901.83 apart — 28.06% — which is far from settled: the seed decides more here than the budget does, so read this total as a good one we found, not as the answer.

    Note: the cost figures above are not proven optimal — the solver ran out of budget with a gap of 89.6% still open, so a cheaper schedule may exist and could be up to that much cheaper.

    [rendered by: template — no evidence chain — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT intent=term-explanation route=term-explanation records=0]
Q[46]: what do you mean seed
  route=term-explanation  source=parse  conf=0.98  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.98  concept=UNRESOLVED<-utterance("seed")  1661ms
  expect: intent=term-explanation route=term-explanation records=0  -> MET
    compared: body=8ad786470c87  records=0  turn=2 of this conversation
  A:
    "seed" — A seed is the starting number for the solver's search. The same plant solved twice with different seeds gives two different schedules, both legal, and usually not the same price — so this board is not "the" answer, it is the cheapest of several runs we made on purpose.

      [R-BK1] the published board is a portfolio, not a draw: K deterministic runs at consecutive seeds, best by ledger, ties by lowest seed

    On this plan: 3 searches were run, at seeds 42–44, and this board is the one from seed 44.

    Next: Ask "is this the cheapest possible plan".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT intent=term-explanation route=term-explanation records=0]
Q[49]: you mentioned seed 44 is the cheapest seed but what does it mean to change seeds
  route=term-explanation  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.95  followup=deepen  concept=UNRESOLVED<-utterance("seeds")  1648ms
  expect: intent=term-explanation route=term-explanation records=0  -> MET
    compared: body=8ad786470c87  records=0  turn=3 of this conversation
  A:
    "seed" — A seed is the starting number for the solver's search. The same plant solved twice with different seeds gives two different schedules, both legal, and usually not the same price — so this board is not "the" answer, it is the cheapest of several runs we made on purpose.

      [R-BK1] the published board is a portfolio, not a draw: K deterministic runs at consecutive seeds, best by ledger, ties by lowest seed

    On this plan: 3 searches were run, at seeds 42–44, and this board is the one from seed 44.

    Next: Ask "is this the cheapest possible plan".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# B — TWO ASKINGS OF ONE WORD ARE ONE ANSWER (BODY_SAME_AS)
# 
# The glossary is authored copy keyed by term, so two phrasings of "what is a
# gap" must produce the SAME body. If they ever differ, something is composing
# a definition rather than retrieving one — which is the failure R-TE1 exists to
# prevent, and nothing else in the suite would notice it.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=solve-optimality route=solve-optimality]
Q[62]: is this the cheapest possible plan
  route=solve-optimality  source=parse  conf=0.95  register=testimony  renderer=template — no evidence chain — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=solve-optimality  conf=0.95  1397ms
  expect: intent=solve-optimality route=solve-optimality  -> MET
  A:
    I can't prove it, and I'd rather say so than guess. The solver ran out of budget before it could close the bound, with a gap of 89.6% still open. The earliest-start tiebreak did not prove out, which does not affect the cost claim above.

    That gap is the limit of the PROOF, not a measure of how good the schedule is: it says a cheaper plan may exist and could be up to 89.6% cheaper. On large plants we have measured wide gaps over schedules whose actual cost barely moved.

    On "found": this board is the best of 3 seeded searches, the one at seed 44.
    What each reached: seed 42 $2,135,369.63, seed 43 $1,801,222.70, seed 44 $1,667,467.80.
    They land $467,901.83 apart — 28.06% — which is far from settled: the seed decides more here than the budget does, so read this total as a good one we found, not as the answer.

    Note: the cost figures above are not proven optimal — the solver ran out of budget with a gap of 89.6% still open, so a cheaper schedule may exist and could be up to that much cheaper.

    [rendered by: template — no evidence chain — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=term-explanation records=0]
Q[65]: what do you mean gap
  route=term-explanation  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.95  concept=UNRESOLVED<-utterance("gap")  1723ms
  expect: route=term-explanation records=0  -> MET
    compared: body=9c3bbe5e5b11  records=0  turn=2 of this conversation
  A:
    "gap" — The gap is how much room is left in the PROOF, not a score for the schedule. The solver holds a plan it found and a bound on what is conceivably possible; the gap is the distance between them. A large gap says we could not prove no cheaper plan exists — it does not say this one is bad.

      [docs/05:Acceptance gates] what this product treats as a proof obligation

    On this plan: the gap is 89.6%.

    Next: Ask "is this the cheapest possible plan".  Ask "how long did the solve take".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=term-explanation records=0 body_same_as=2]
Q[68]: what does the gap mean
  route=term-explanation  source=parse  conf=0.98  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.98  followup=deepen  concept=UNRESOLVED<-utterance("gap")  1608ms
  expect: route=term-explanation records=0 body_same_as=2  -> MET
    compared: body=9c3bbe5e5b11  records=0  turn=3 of this conversation
  A:
    "gap" — The gap is how much room is left in the PROOF, not a score for the schedule. The solver holds a plan it found and a bound on what is conceivably possible; the gap is the distance between them. A large gap says we could not prove no cheaper plan exists — it does not say this one is bad.

      [docs/05:Acceptance gates] what this product treats as a proof obligation

    On this plan: the gap is 89.6%.

    Next: Ask "is this the cheapest possible plan".  Ask "how long did the solve take".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# C — TWO DIFFERENT WORDS ARE TWO DIFFERENT ANSWERS (BODY_DIFFERS_FROM)
# 
# The shared-body defect the census micro-session found, watched at the new
# route before it can happen: one assembler serving two questions is exactly how
# `certificate-testimony` and `data-problems` came to render one body.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=solve-optimality route=solve-optimality]
Q[80]: is this the cheapest possible plan
  route=solve-optimality  source=parse  conf=0.95  register=testimony  renderer=template — no evidence chain — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=solve-optimality  conf=0.95  1104ms
  expect: intent=solve-optimality route=solve-optimality  -> MET
  A:
    I can't prove it, and I'd rather say so than guess. The solver ran out of budget before it could close the bound, with a gap of 89.6% still open. The earliest-start tiebreak did not prove out, which does not affect the cost claim above.

    That gap is the limit of the PROOF, not a measure of how good the schedule is: it says a cheaper plan may exist and could be up to 89.6% cheaper. On large plants we have measured wide gaps over schedules whose actual cost barely moved.

    On "found": this board is the best of 3 seeded searches, the one at seed 44.
    What each reached: seed 42 $2,135,369.63, seed 43 $1,801,222.70, seed 44 $1,667,467.80.
    They land $467,901.83 apart — 28.06% — which is far from settled: the seed decides more here than the budget does, so read this total as a good one we found, not as the answer.

    Note: the cost figures above are not proven optimal — the solver ran out of budget with a gap of 89.6% still open, so a cheaper schedule may exist and could be up to that much cheaper.

    [rendered by: template — no evidence chain — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=term-explanation records=0]
Q[83]: what do you mean seed
  route=term-explanation  source=parse  conf=0.98  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.98  concept=UNRESOLVED<-utterance("seed")  1453ms
  expect: route=term-explanation records=0  -> MET
    compared: body=8ad786470c87  records=0  turn=2 of this conversation
  A:
    "seed" — A seed is the starting number for the solver's search. The same plant solved twice with different seeds gives two different schedules, both legal, and usually not the same price — so this board is not "the" answer, it is the cheapest of several runs we made on purpose.

      [R-BK1] the published board is a portfolio, not a draw: K deterministic runs at consecutive seeds, best by ledger, ties by lowest seed

    On this plan: 3 searches were run, at seeds 42–44, and this board is the one from seed 44.

    Next: Ask "is this the cheapest possible plan".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=term-explanation records=0 body_differs_from=2]
Q[86]: what do you mean gap
  route=term-explanation  source=parse  conf=0.98  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.98  followup=deepen  concept=UNRESOLVED<-utterance("gap")  1444ms
  expect: route=term-explanation records=0 body_differs_from=2  -> MET
    compared: body=9c3bbe5e5b11  records=0  turn=3 of this conversation
  A:
    "gap" — The gap is how much room is left in the PROOF, not a score for the schedule. The solver holds a plan it found and a bound on what is conceivably possible; the gap is the distance between them. A large gap says we could not prove no cheaper plan exists — it does not say this one is bad.

      [docs/05:Acceptance gates] what this product treats as a proof obligation

    On this plan: the gap is 89.6%.

    Next: Ask "is this the cheapest possible plan".  Ask "how long did the solve take".
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# D — THE SCOPE WALL, GRADED (the true negative)
# 
# THE SAME QUESTION AS BLOCK A's SECOND TURN, WITH NO OPENER. Nothing has said
# "seed" to this planner, so this is not the intent — the route must be anything
# BUT `term-explanation`. Without this block the bank grades a route that fires;
# with it, the bank grades a route that KNOWS WHEN NOT TO.
# 
# The expectation is deliberately NOT a named route. What the second tier does
# with an unconfirmed proposal is its business and it varies (the (d.3) census
# measured these questions reaching four different second-tier shapes at v18);
# what R-TE1 rules is that the glossary does not answer.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=term-explanation route=synthesis|CLARIFY|coaching|prove-it]
Q[104]: what do you mean seed
  route=synthesis  source=parse  conf=0.98  register=synthesis  renderer=synthesis (claude-sonnet-5) — 1 tool call(s)
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=term-explanation  conf=0.98  concept=UNRESOLVED<-utterance("seed")  1545ms
  synthesis: claims=4  verified=0  interpretive=0  general-knowledge=1  cut=3  ungrounded-load-bearing=3  tools=1(spec_lookup)  8909ms
  expect: intent=term-explanation route=synthesis|CLARIFY|coaching|prove-it  -> MET
  A:
    In scheduling and optimization generally, a 'seed' usually means the starting point an algorithm is initialized with — either a random seed that makes a solver's search reproducible, or a seed solution (an initial schedule, often the previous plan) that a re-solve is built from or compared against.  [general knowledge — how scheduling works in general, not a fact about this plan]

    Where a line is marked general knowledge it draws on how scheduling and plants behave generally — not on this plan's records, so there is nothing here to check it against.

    One step of my reasoning didn't hold up against the records, so I couldn't ground part of it and I've left that step out rather than state it.
    [rendered by: synthesis (claude-sonnet-5) — 1 tool call(s) | register: synthesis]
  >> sidecar[ungrounded-load-bearing]: 3 load-bearing claim(s) could not be grounded and were cut
------------------------------------------------------------------------
# ===========================================================================
# E — A WORD WE DO NOT DEFINE (the other half of the wall)
# 
# "bottleneck" is a perfectly good scheduling term and it is not OURS. It goes
# to the second tier and is answered as general knowledge under R-TG1's label,
# which is the correct home for it — the glossary must not grow a definition for
# every word a planner might type. This is the documentation-browser slope, and
# this block is the guard on it.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=solve-optimality route=solve-optimality]
Q[118]: is this the cheapest possible plan
  route=solve-optimality  source=parse  conf=0.95  register=testimony  renderer=template — no evidence chain — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=solve-optimality  conf=0.95  1160ms
  expect: intent=solve-optimality route=solve-optimality  -> MET
  A:
    I can't prove it, and I'd rather say so than guess. The solver ran out of budget before it could close the bound, with a gap of 89.6% still open. The earliest-start tiebreak did not prove out, which does not affect the cost claim above.

    That gap is the limit of the PROOF, not a measure of how good the schedule is: it says a cheaper plan may exist and could be up to 89.6% cheaper. On large plants we have measured wide gaps over schedules whose actual cost barely moved.

    On "found": this board is the best of 3 seeded searches, the one at seed 44.
    What each reached: seed 42 $2,135,369.63, seed 43 $1,801,222.70, seed 44 $1,667,467.80.
    They land $467,901.83 apart — 28.06% — which is far from settled: the seed decides more here than the budget does, so read this total as a good one we found, not as the answer.

    Note: the cost figures above are not proven optimal — the solver ran out of budget with a gap of 89.6% still open, so a cheaper schedule may exist and could be up to that much cheaper.

    [rendered by: template — no evidence chain — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=synthesis|CLARIFY|coaching|prove-it]
Q[121]: what do you mean bottleneck
  route=synthesis  source=parse  conf=0.95  register=synthesis  renderer=synthesis (claude-sonnet-5) — 7 tool call(s)
  lit-bars=4  records=2  refs=2/2/0
  parse: intent=term-explanation  conf=0.95  followup=deepen  concept=UNRESOLVED<-utterance("bottleneck")  2050ms
  synthesis: claims=4  verified=0  interpretive=3  general-knowledge=0  cut=1  ungrounded-load-bearing=1  tools=7(spec_lookup,lateness_set,constraint_catalog,cost_ledger,fetch_record,placements_for_order,machine_occupancy)  45019ms
  expect: route=synthesis|CLARIFY|coaching|prove-it  -> MET
  A:
    On this board, ORD-000091's paint operation waited from 8 January (when its two upstream ops on CUT-02 and MILL-01 finished) until 15 January to run on PAINT-01 — a wait of about a week — and that order ended up costing 147,776.67 in tardiness, the largest single tardiness line in the ledger.  [synthesis — read from: febd9019, 5b1ff8de, 6266f174]
    PAINT-01 is running at 79.4% utilization overall (12,410 working minutes against 15,626 open minutes), and its queue on the board is packed nearly back-to-back through most days — many operations there show 0 minutes of open gap before they start. (based on the 60 row(s) lateness_set returned, not the whole plan)  [synthesis — read from: 7c78eed8]
    That pattern — a heavily loaded machine with little slack, sitting ahead of an order that then finishes weeks late — is what "bottleneck" points at on a real board: PAINT-01 looks like a strong candidate for that role for ORD-000091, though I only traced one order's path and haven't checked every machine's occupancy to say it's the plant-wide bottleneck. (based on the 60 row(s) lateness_set returned, not the whole plan)  [synthesis — read from: 849c0ff5, 5efee06d, a740525e]

    One step of my reasoning didn't hold up against the records, so I couldn't ground part of it and I've left that step out rather than state it.
    [rendered by: synthesis (claude-sonnet-5) — 7 tool call(s) | register: synthesis]
  >> sidecar[ungrounded-load-bearing]: 1 load-bearing claim(s) could not be grounded and were cut
------------------------------------------------------------------------
# ===========================================================================
# F — THE TWO SEAMS THE SPECIMENS WERE WRONGLY CAUGHT BY, STILL WORKING
# 
# A fix that empties the entity clarify or the capability coach has traded one
# wrong answer for another. Both must still answer the questions they are for.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT route=CLARIFY]
Q[132]: why cant this be moved earlier
  route=CLARIFY  source=parse  conf=0.92  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=why-here  conf=0.92  polarity=negative  order=UNRESOLVED<-utterance("this")  clarify=no-subject  2269ms
  expect: route=CLARIFY  -> MET
  A:
    I need one more detail to answer "why cant this be moved earlier".
    Which order, machine, or customer do you mean? Ask it again naming one, e.g. "why is that order late?" becomes "why is <order> late?".

    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [RESET — conversation cleared]
# [EXPECT intent=coaching route=coaching]
Q[136]: how do i turn on overtime
  route=coaching  source=parse  conf=0.98  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=coaching  conf=0.98  concept=overtime<-utterance("overtime")  2453ms
  expect: intent=coaching route=coaching  -> MET
  A:
    Yes — extra working hours beyond the regular shift — an overtime window a job can run in when the regular calendar is closed, priced at a premium so the schedule only buys those hours when they pay for themselves (e.g. a Saturday shift that rescues a tight order).

    To enable it: add an added exception row with reason overtime on that resource's calendar in calendars.csv (the window it opens), and set an overtime_premium_multiplier in cost_model.json (the premium its hours bill at). See the incoming-data spec §5.6.

    Want to check what the submission already declares? Ask "what data problems exist?"

    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
