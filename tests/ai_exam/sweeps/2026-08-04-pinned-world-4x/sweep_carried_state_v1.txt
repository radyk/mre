AI EXAM TRANSCRIPT
target      : rolling-c32a6140-b6b
snapshot    : snap-rolling
started     : 2026-08-05T01:27:58Z
llm mode    : live
questions   : 16
llm calls   : 27
door check  : live
parse       : parses=18 calls=18 retries=0 malformed=0 clarifies=0 unavailable=0 median=1387ms
synthesis   : answers=2 calls=8 tools=6 malformed=0 budget-out=0 unanswerable=1 median=21344ms p90=40327ms
claims      : 8 total  verified=0 interpretive=2 general-knowledge=4 failed-and-cut=2 deferred=0  ungrounded-load-bearing=2
latency     : route n=14 median=1783ms p90=2494ms | synthesis n=2 median=23205ms p90=42484ms
graded      : 14/15 expectations met
sidecar     : expect-miss=1, ungrounded-load-bearing=1
------------------------------------------------------------------------
# AI EXAM BANK — WHAT THE PRODUCT KEEPS FROM THE LAST TURN (R-MT1, R-LD5)
# version  : v1
# generated: 2026-08-04  (Session 4A teaching-graft (d.1))
# world    : the DEMO BOARD `rolling-c32a6140-b6b`. Entity ids below are that
# board's; the SHAPES are what is graded, not the ids.
# 
# ===========================================================================
# WHAT THIS BANK CAN GRADE, AND WHAT IT CANNOT — READ THIS FIRST
# ===========================================================================
# 
# This is the measured input to Q7 (the bank-format decision, docs/07 §5b), and
# it is written down here rather than in a design note because the gap is a
# property of THESE probes, not a hypothesis.
# 
# The exam grammar's `EXPECT` grades EIGHT KEYS — intent, route, order, machine,
# concept, followup, polarity, clarify — and every one of them is a property of
# ONE TURN'S PARSE. Nothing in the format can say "turn 2 opened turn 1's
# records", "turn 2's answer names turn 1's question", or "turn 2 did NOT open
# the board's worst gate finding". So of the five sequences below:
# 
# * the ROUTING half of all five is expressible and is graded here. That is
# not nothing — D-01's specimen (P8 T2) reached `drill-down` correctly and
# was still wrong, and the shape of the *previous* turn is what decides
# whether the route can be right, which is why these are sequences and not
# independent probes;
# * the GROUNDING half of all five is NOT expressible and is asserted in
# `tests/test_carried_answer_state.py` over two consecutive `run_ask` calls,
# plus quoted verbatim from live runs in the close-out;
# * THE CROSS-VERSION PAIR (recon P4, R-MT1's founding specimen) CANNOT BE
# ENCODED AT ALL. A run targets ONE schedule — `RunTarget` is resolved once
# — and the grammar has no directive that rebinds the board mid-conversation.
# That is not a missing EXPECT key; it is a missing world. It is measured in
# `tools/spikes/teaching_graft_d1/after.py p4` and pinned in the guard file's
# `Panel.rebind`, and it is the single strongest argument in this session for
# whatever Q7 decides.
# 
# One more thing the format cannot say, found while writing these: an assertion
# about what a turn did NOT do. "the drill-down opened nothing" and "the
# drill-down opened the wrong thing" are the same EXPECT line.
# 
# RESET DISCIPLINE. Each BLOCK is one conversation and is separated by RESET;
# inside a block the turns are a THREAD ON PURPOSE, which is the opposite of
# every teaching bank's discipline and is why this file is separate from them.
# ===========================================================================
# ===========================================================================
# A — THE DRILL-DOWN, WIRED (D-01)
# 
# The recon's P8, verbatim. Turn 1 is a teaching question; turn 2 is the most
# natural follow-up a planner types. Before this session turn 2 returned the
# board's most severe DATA-QUALITY finding — a gate warning about CUT-01 — with
# nothing to do with frozen zones, ORD-000209, or anything turn 1 said.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=teaching route=synthesis]
Q[57]: how does a frozen zone normally work on a board like this
  route=synthesis  source=parse  conf=0.95  register=synthesis  renderer=synthesis (claude-sonnet-5) — 6 tool call(s)
  lit-bars=4  records=2  refs=2/2/0
  parse: intent=teaching  conf=0.95  concept=UNRESOLVED<-utterance("frozen zone")  1751ms
  synthesis: claims=8  verified=0  interpretive=2  general-knowledge=4  cut=2  ungrounded-load-bearing=2  tools=6(spec_lookup,constraint_catalog,lateness_set,gate_findings,placements_for_order,fetch_record)  40327ms
  expect: intent=teaching route=synthesis  -> MET
  A:
    No contracted answer covers that one, so this is me reading the evidence directly — each line below says what backs it.

    On a rolling board, the frozen zone is the near-term slice of the schedule that is locked and will not be re-touched as the plan rolls forward — the spec's word for this is 'committed': work placed inside the frozen zone is static, not just currently-scheduled.  [general knowledge — how scheduling works in general, not a fact about this plan]
    Just outside the frozen zone sits the 'active window' — work that is placed and solved in the current pass but can still move on the next roll — and further out is 'beyond-horizon' work: known and admitted, but with no placement yet in this scheduling window, which is normal and not a sign anything was dropped.  [general knowledge — how scheduling works in general, not a fact about this plan]
    The mechanical purpose of freezing a near-term zone is stability: it stops the solver from re-shuffling jobs that are about to run or are already in setup, which would otherwise happen every time the plan re-solves on a rolling horizon.  [general knowledge — how scheduling works in general, not a fact about this plan]
    Freezing is implemented through explicit lock records — an order id, optional sequence, resource, start time and a lock_type such as 'frozen' (immovable), 'pinned_resource', or 'pinned_start' — each carrying an authority (who imposed it) and an optional expiry, so an override decays rather than accreting silently.  [general knowledge — how scheduling works in general, not a fact about this plan]
    On this board, ORD-000206 is scheduled with two operations — CUT-01 on 2026-01-15 and PAINT-02 on 2026-02-02 — against a due date of 2025-12-25, so it is showing as late by 55,860 minutes; this is the kind of order a frozen zone's boundary decides whether to touch on the next roll.  [synthesis — read from: bf70bf3d, a43a34ab, a740525e]
    I could not find a disposition field (committed / active_window / beyond-horizon) on the order record itself or in the placements, so I cannot say from this board which of ORD-000206's two operations, if any, currently sits inside the frozen zone — that would need a locks.csv-style record, which this run's evidence tools did not surface for this order.  [synthesis — read from: a740525e]

    Where a line is marked general knowledge it draws on how scheduling and plants behave generally — not on this plan's records, so there is nothing here to check it against.

    Part of what I drafted was neither something I could check against your board nor general scheduling knowledge, so I left it out rather than state it with a label that would have been wrong either way.

    That is how it works in general — you know this plant and I don't, so if any of it doesn't match what you see here, say so and I'll look at what your board actually does.
    [rendered by: synthesis (claude-sonnet-5) — 6 tool call(s) | register: synthesis]
  >> sidecar[ungrounded-load-bearing]: 2 load-bearing claim(s) could not be grounded and were cut
------------------------------------------------------------------------
# [EXPECT route=drill-down]
Q[60]: can you show me that on my board
  route=drill-down  source=parse  conf=0.92  register=synthesis  renderer=synthesis (grounding pass)
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=drill-down  conf=0.92  followup=deepen  1048ms
  expect: route=drill-down  -> MET
  A:
    That line wasn't about this plan — it's general scheduling knowledge, so there is no record of yours behind it and nothing here to check it against. If you want the same question answered from this board, say so and I'll read it.

    The claim: "On a rolling board, the frozen zone is the near-term slice of the schedule that is locked and will not be re-touched as the plan rolls forward — the spec's word for this is 'committed': work placed inside the frozen zone is static, not just currently-scheduled."
    [rendered by: synthesis (grounding pass) | register: synthesis]
------------------------------------------------------------------------
# [EXPECT intent=frozen route=frozen]
Q[63]: why doesn't that apply to ORD-000128
  route=frozen  source=parse  conf=0.92  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=frozen  conf=0.92  followup=deepen  polarity=negative  order=ORD-000128<-utterance("ORD-000128")  2213ms
  expect: intent=frozen route=frozen  -> MET
  A:
    ORD-000128 is placed, but after the frozen boundary (2026-01-06) — it sits in the active part of this window, which runs to 2026-01-15. That is the whole difference: work before the boundary is committed and will not move, work after it is still being solved and can move as the schedule rolls.
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# B — THE OTHER PHRASING OF THE SAME GESTURE
# 
# "show me the evidence for that" and "can you show me that on my board" are one
# gesture. They were two intents, one wired and one not; they now ground on the
# same two stores in the same order. The routes still differ — that is the
# PARSE's reading and this bank does not argue with it — but what opens must be
# the same object, which is the half only the guard file can state.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=why-here route=why-here]
Q[77]: why is ORD-000128 op20 placed where it is
  route=why-here  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=why-here  conf=0.95  order=ORD-000128<-utterance("ORD-000128")  1392ms
  expect: intent=why-here route=why-here  -> MET
  A:
    ORD-000128 op20 couldn't start before Tuesday 2026-01-13 16:09: op10 finishes at 2026-01-13 16:08.
    Before that: its release date is 2025-12-04 00:00.

    What pushed it, in order:
      2025-12-04 00:00  release date [docs/05 A4] — its release date is 2025-12-04 00:00
      2026-01-13 16:08  an earlier step [docs/05 A1/A2] — op10 finishes at 2026-01-13 16:08

    Not weighed here (docs/05): B3/B5 secondary and cumulative resources (tools, operator pools); B7/B8 sequence-dependent changeover; C4 time-window operation restrictions; F3 SameResource linkage.
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT followup=prove-it]
Q[80]: show me the evidence for that
  interpreted as: (resolved against ORD-000128 (read from what you asked earlier — you didn't name it in this question))
  route=prove-it  source=parse  conf=0.95  register=synthesis  renderer=synthesis (grounding pass)
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=prove-it  conf=0.95  followup=prove-it  order=ORD-000128<-conversation("ORD-000128")  1320ms
  expect: followup=prove-it  -> MET
  A:
    That was my answer to "why is ORD-000128 op20 placed where it is". It came from a contracted route, so it has no per-sentence claims to pick apart — here is the whole record set it was assembled from (1 record(s)):

      - the assignment decision — recorded driver: the machine was busy with other work  [record: 5e4e30f3...]
    [rendered by: synthesis (grounding pass) | register: synthesis]
------------------------------------------------------------------------
# ===========================================================================
# C — THE REFUSAL HALF (D-01, and the more important half)
# 
# A cold drill-down: nothing has been answered, so there is nothing to open. The
# answer must SAY SO and offer the door. A default that asserts manufactures a
# claim out of a gap — and on this board the gap it used to fill was a WARNING.
# 
# THIS EXPECTATION IS A KNOWN, UNDERSTOOD MISS AND IT IS KEPT RATHER THAN
# FITTED. Measured live: with no conversation at all the parse reads these words
# as `unmatched` at confidence 0.25 and the second tier answers — honestly, with
# zero tool calls and the capability card. That is a GOOD answer and arguably a
# better one than the route's own refusal, but it means the cold refusal on the
# `drill-down` ROUTE is unreachable from a live parse and is proven only where
# the parse is scripted (`tests/test_carried_answer_state.py::
# TestDrillDownRefusal`). The probe is kept because what it grades — that a cold
# "show me that" never opens the board's worst gate finding — is true on both
# paths, and rewriting the expectation to match the parse would hide the fact
# that two different mechanisms are keeping it true.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT route=drill-down]
Q[104]: can you show me that on my board
  route=synthesis  source=parse  conf=0.15  register=synthesis  renderer=synthesis (claude-sonnet-5) — 0 tool call(s)
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=unmatched  conf=0.15  1496ms
  synthesis: claims=0  verified=0  interpretive=0  general-knowledge=0  cut=0  tools=0  unanswerable  2361ms
  expect: route=drill-down  -> MISSED
  A:
    I couldn't answer that one: I don't have a tool that reaches it. Nothing I can read holds that, so I'd rather say so than guess.

    Here's what I can do that's closest:
      - show every late order at a glance
      - list the data-quality problems
    [rendered by: synthesis (claude-sonnet-5) — 0 tool call(s) | register: synthesis]
  >> sidecar[expect-miss]: route: expected 'drill-down', got 'synthesis'
------------------------------------------------------------------------
# ===========================================================================
# D — THE SUBJECT NOBODY NAMED (R-LD5)
# 
# The recon's P2b middle arm. Nothing is selected, so every rung of the
# resolution ladder is empty; the PARSE recovers ORD-000073 and its op grain
# from the previous question's TEXT. The routing here is what makes the
# disclosure necessary — an answer about an order the planner did not name in
# that sentence — and the disclosure itself is asserted in the guard file and
# quoted live in the close-out.
# 
# THE TRUE NEGATIVE IS THE SECOND BLOCK: the same words with an UNRELATED turn
# before them must reach CLARIFY, because there is genuinely nothing to bind.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=why-here route=why-here]
Q[122]: why is ORD-000073 op10 placed where it is
  route=why-here  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=why-here  conf=0.95  order=ORD-000073<-utterance("ORD-000073")  1897ms
  expect: intent=why-here route=why-here  -> MET
  A:
    I can't attribute ORD-000073 op10's placement at Monday 2026-01-05 07:00 to a binding constraint: the earliest-start estimates I can compute don't line up with where it actually sits. Rather than pick one and sound certain, I'd rather say I don't know.

    What pushed it, in order:
      2026-01-05 00:00  release date [docs/05 A4] — its release date is 2026-01-05 00:00
      2026-01-06 00:00  the frozen boundary [docs/05 R-F1] — the frozen boundary stands at 2026-01-06 00:00
      2026-01-06 07:00  the machine's calendar [docs/05 C1/C2] — PRESS-FAST is closed until 2026-01-06 07:00
      2026-01-23 12:25  no window long enough [docs/05 C3] — it needs 4h05m in one piece and PRESS-FAST had only 2h09m left when it came free at 2026-01-22 16:51
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
Q[124]: why cant this be moved earlier
  interpreted as: (resolved against ORD-000073 (read from what you asked earlier — you didn't name it in this question); and about op10, read from your earlier question)
  route=what-would-change  source=parse  conf=0.92  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=what-would-change  conf=0.92  followup=deepen  order=ORD-000073<-conversation("ORD-000073")  1382ms
  A:
    I can't tell you what would move ORD-000073 op10: the earliest-start estimates I can compute don't line up with where it actually sits, so I don't know which of them to relax. Rather than pick one and sound certain, I'd rather say I don't know.

    Not weighed here (docs/05): B3/B5 secondary and cumulative resources (tools, operator pools); B7/B8 sequence-dependent changeover; C4 time-window operation restrictions; F3 SameResource linkage.
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [RESET — conversation cleared]
# [EXPECT intent=late-orders route=late-orders]
Q[128]: how many orders are late
  route=late-orders  source=parse  conf=0.99  register=testimony  renderer=LLM (claude-haiku-4-5-20251001)
  lit-bars=102  records=102  refs=0/0/102
  parse: intent=late-orders  conf=0.99  1048ms
  expect: intent=late-orders route=late-orders  -> MET
  A:
    Yes, there are 102 late orders in this schedule. Of the 280 orders known to this plan, 158 are scheduled in this window, and 102 of those scheduled orders are late [record: c0479435...]. The total lateness added by this schedule is 1477827 minute(s), with 626400 minute(s) representing unavoidable lateness that existed before the window opened [record: c0479435...].
    [rendered by: LLM (claude-haiku-4-5-20251001) | register: testimony]
------------------------------------------------------------------------
# NB the expectation is on the ROUTE, not on `clarify=`. Measured: the parse
# emits `intent=why-here conf=0.92` with the order UNRESOLVED and NO clarify
# payload; the CLARIFY comes from the dispatch's subject-resolution guard. The
# `clarify=` key grades the PARSE's own reason, which is correctly None here.
# [EXPECT route=CLARIFY]
Q[135]: why cant this be moved earlier
  route=CLARIFY  source=parse  conf=0.92  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=why-here  conf=0.92  followup=deepen  polarity=negative  order=UNRESOLVED<-utterance("this")  1321ms
  expect: route=CLARIFY  -> MET
  A:
    I need one more detail to answer "why cant this be moved earlier".
    Which order, machine, or customer do you mean? Ask it again naming one, e.g. "why is that order late?" becomes "why is <order> late?".

    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# E — THE DEAF GATE, BOTH SIDES (D-07)
# 
# E1 is the FALSE POSITIVE the gate removes: a deictic follow-up that correctly
# re-resolves to the same subject and correctly gets the same answer, which the
# rider used to answer with "I'm not understanding what you're asking".
# 
# E2 WAS THE TRUE POSITIVE, AND IT IS NOW EXTINCT BY REPAIR — READ THIS BEFORE
# "RESTORING" ANYTHING. (d.0) P6 T7 was the first true positive `deaf` produced
# in this repo's record: different question, and the same body, because
# `certificate-testimony` and `data-problems` rendered the same answer.
# 
# THE SHARED-BODY CENSUS MICRO-SESSION FIXED THE ROUTE. `certificate-testimony`
# now states the certificate, so the two bodies differ and `deaf` is correctly
# SILENT on this pair. The earlier version of this comment said "if E2 ever
# stops firing, the gate has become a suppression" — that reading is now WRONG
# and is corrected here rather than left to mislead: the gate is untouched, and
# a true positive whose defect has been repaired SHOULD go quiet.
# 
# What this bank still pins is the ROUTING — two different questions must keep
# reaching two different routes. The BODIES are asserted in
# `tests/test_certificate_route.py`, because no EXPECT key can state body
# content (see the header's note on what this format cannot say).
# ===========================================================================
# [RESET — conversation cleared]
# [SELECT order=ORD-000128 op_seq=20]
# [EXPECT intent=why-here route=why-here]
Q[165]: why is ORD-000128 op20 placed where it is
  selection: order=ORD-000128, op_seq=20
  interpreted as: (resolved against ORD-000128 (from board selection))
  route=why-here  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=why-here  conf=0.95  order=ORD-000128<-selection("ORD-000128")  1407ms
  expect: intent=why-here route=why-here  -> MET
  A:
    ORD-000128 op20 couldn't start before Tuesday 2026-01-13 16:09: op10 finishes at 2026-01-13 16:08.
    Before that: its release date is 2025-12-04 00:00.

    What pushed it, in order:
      2025-12-04 00:00  release date [docs/05 A4] — its release date is 2025-12-04 00:00
      2026-01-13 16:08  an earlier step [docs/05 A1/A2] — op10 finishes at 2026-01-13 16:08

    Not weighed here (docs/05): B3/B5 secondary and cumulative resources (tools, operator pools); B7/B8 sequence-dependent changeover; C4 time-window operation restrictions; F3 SameResource linkage.
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT route=why-here]
Q[168]: so why is it there
  selection: order=ORD-000128, op_seq=20
  interpreted as: (resolved against ORD-000128 (from board selection))
  route=why-here  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=2  records=1  refs=1/1/0
  parse: intent=why-here  conf=0.95  followup=deepen  order=ORD-000128<-selection("ORD-000128")  1372ms
  expect: route=why-here  -> MET
  A:
    ORD-000128 op20 couldn't start before Tuesday 2026-01-13 16:09: op10 finishes at 2026-01-13 16:08.
    Before that: its release date is 2025-12-04 00:00.

    What pushed it, in order:
      2025-12-04 00:00  release date [docs/05 A4] — its release date is 2025-12-04 00:00
      2026-01-13 16:08  an earlier step [docs/05 A1/A2] — op10 finishes at 2026-01-13 16:08

    Not weighed here (docs/05): B3/B5 secondary and cumulative resources (tools, operator pools); B7/B8 sequence-dependent changeover; C4 time-window operation restrictions; F3 SameResource linkage.
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [RESET — conversation cleared]
# [EXPECT intent=data-problems route=data-problems]
Q[172]: are there any data quality problems
  route=data-problems  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=50  records=4  refs=0/3/47
  parse: intent=data-problems  conf=0.95  1054ms
  expect: intent=data-problems route=data-problems  -> MET
  A:
    4 data-quality problem(s):

      1. CUT-01 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-01
      2. CUT-02 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-02
      3. CUT-03 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-03
      4. 47 orders was already past its due date before this plan started, and is scheduled late  [INFO]
           Affected: ORD-000001, ORD-000004, ORD-000009 … 47 in all

    Want the fix-first ordering? Ask "what should I fix first?"
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT intent=certificate-testimony route=certificate-testimony]
Q[175]: what does the certificate say
  route=certificate-testimony  source=parse  conf=0.95  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=50  records=4  refs=0/3/47
  parse: intent=certificate-testimony  conf=0.95  followup=deepen  1552ms
  expect: intent=certificate-testimony route=certificate-testimony  -> MET
  A:
    This plan has no certificate. The submission never went through the intake gate, so no grade was ever issued — there is nothing for me to report and nothing for me to guess.

    Behind that, the problems on the record:

    4 data-quality problem(s):

      1. CUT-01 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-01
      2. CUT-02 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-02
      3. CUT-03 is in a workload too dense to schedule cleanly  [WARNING]
           Affected: CUT-03
      4. 47 orders was already past its due date before this plan started, and is scheduled late  [INFO]
           Affected: ORD-000001, ORD-000004, ORD-000009 … 47 in all

    Want the fix-first ordering? Ask "what should I fix first?"
    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# ===========================================================================
# F — WHICH KIND OF NOTHING (D-06)
# 
# `advice` reads the plan (since 4B.22 B2 it leads with the opener's top-ranked
# item) and attaches no records. A prove-it on it used to say "authored copy —
# it states what this product can and can't do", which is false of it. The
# graded half is the routing; the sentence is in the guard file and quoted live.
# ===========================================================================
# [RESET — conversation cleared]
# [EXPECT intent=advice route=advice]
Q[188]: if i could fix one thing what should it be
  route=advice  source=parse  conf=0.92  register=testimony  renderer=template — authored copy — rendered verbatim
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=advice  conf=0.92  1221ms
  expect: intent=advice route=advice  -> MET
  A:
    The first thing worth your attention on this board, ranked by consequence out of 6:

      The cost optimum is NOT proved — the solver stopped with a gap of 89.6% still open.
        Against a ledger of 1,667,467.80, that bound leaves up to 1,494,205.31 on the table — an upper limit on what a cheaper schedule could save, not a saving anyone has found.
        -> Ask "is this schedule optimal?" for what the solver did and did not prove.

    That is a ranking of what is worth looking at, not a recommendation of what to do about it.

    102 order(s) finish late in this plan. I can't recommend an intervention yet — deciding whether to open overtime, add a machine, or re-prioritise isn't a question I answer today.

    Here's what I can do to help you decide:
      - explain why any order is late ("why is <order> late?")
      - show what an order is waiting on ("why can't <order> start earlier?")
      - price a what-if on the board: drag a job and I'll cost the move exactly.

    My take: ORD-000112's 27060-minute slip traces to ORD-000252 holding CUT-01 until 2026-01-27 19:00 — pulling that earlier is the single biggest lever the board gives you today.

    [rendered by: template — authored copy — rendered verbatim | register: testimony]
------------------------------------------------------------------------
# [EXPECT followup=prove-it]
Q[191]: show me the evidence for that
  route=prove-it  source=parse  conf=0.95  register=synthesis  renderer=synthesis (grounding pass)
  lit-bars=0  records=0  refs=0/0/0
  parse: intent=prove-it  conf=0.95  followup=prove-it  2441ms
  expect: followup=prove-it  -> MET
  A:
    My answer to "if i could fix one thing what should it be" came from advice, which answers from this plan — but it attached no records of its own, so there is nothing here for me to open. That is a different thing from it being a statement about what this product can do: it read the board and cited nothing. Ask about a particular order or machine and the answer will cite what it was built from.
    [rendered by: synthesis (grounding pass) | register: synthesis]
------------------------------------------------------------------------
