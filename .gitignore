__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
# Evidence store output (runtime artifacts, not source)
evidence_store/
snapshots/
runs/
mre_output/
mre_demo_output/
mre_output_whatif/
raw_data/
raw_data_profile.md
# API/runtime data root and deploy secrets/artifacts
mre_api_data/
_data/
# Throwaway sabotage-audit dataset copies (dev_audit_sandbox.ps1) — never commit
# an edited copy of a committed dataset (see datasets/glass_box/SABOTAGE_MENU.md)
_sandbox/
# Gate output written beside a committed dataset (runtime artifact)
datasets/**/gate_output/
.env.local
deploy/azure/.env
deploy/.smoke_*/
*_baseline.json
# Session 4A.3b — exam-harness scratch (pinned runs, logs); sweeps/ IS committed
_ai_exam_scratch/
# Session 4B.6c measurement scratch (generated pilot_scale submissions + run dirs)
_4b6c_scratch/
# Session 4B.8 scratch (monolithic accounting runs, doc patch scripts)
_4b8_scratch/
# Session 4B.10 scratch (generated facility_real submissions + run dirs);
# tools/spikes/density_4b10/ and its .jsonl results ARE committed
_4b10_scratch/
# Session 4B.11 scratch (generated facility_real_pastdue submissions + run dirs,
# BEFORE/AFTER probe transcripts, the golden-accounting runs). The probe and the
# floor-invariance spike ARE committed, under tools/spikes/pastdue_4b11/.
_4b11_scratch/
# Session 4B.12 scratch (regenerated facility_real submissions + per-process run
# dirs) and sweep logs; tools/spikes/density_4b12/ and its .jsonl results ARE
# committed, including the quarantined wall-truncated rows (.bak)
_4b12_scratch/
_4b12_logs/
# Session 4B.18 scratch (round-trip measurement, the migrated monolithic copy)
_4b18_scratch/
# Sessions 4B.15a / 4B.20 / 4B.21 / 4B.22 scratch (census output, measurement logs)
_4b15a_scratch/
_4b20_scratch/
_4b21_scratch/
_4b22_scratch/
# Errand 4B.22a scratch (14 generated candidate worlds + their persisted run dirs,
# the minted demo_board submission, sweep and verification logs). The SCRIPTS are
# committed, under tools/spikes/demo_board_4b22a/; their outputs are not.
_4b22a_scratch/
# Session 4B.25 scratch (a COPY of the data root holding only the dense demo
# board, so the audit-accept success branch could be executed without minting a
# schedule row in the working root; plus the portfolio measurement's prepared
# plants). The SCRIPTS are committed, under tools/spikes/portfolio_4b25/, and so
# are their measurement rows.
_4b25_scratch/
# Sessions 4B.26 / 4B.27 / 4B.29 scratch (calibration grids + run dirs; the
# 4B.27 ask-path census output, conversation harness and drag probes)
_4b26_scratch/
_4b27_scratch/
_4b29_scratch/
# Session W2.3 scratch (the gen-3 demo board's live cockpit screenshots). The
# SCRIPTS are committed, under tools/spikes/gen3_demo_world/; the shots are not.
_w23_scratch/
